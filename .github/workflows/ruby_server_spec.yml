name: ruby-server-spec

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Run bundle install
        run: cd http_server_spec pwd
      - name: Print Working Directory
        run: pwd